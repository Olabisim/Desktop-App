import React from 'react';
import { Text as T, View as V, StyleSheet as SS } from 'react-native';

const Home = () => {
        return (
                <V>

                        <T style={{color: 'black'}}>
                                this is my first compoenent in a desktop app
                        </T>

                </V>
        )
}

const styles = SS.create({

})




export default Home;